# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: plutchik-wheel-explorer
metadata:
  template: plutchik-wheel-explorer@0.0.1-beta

services:
  web:
    project: .
    host: containerapp
    language: ts
    docker:
      path: Dockerfile

infra:
  provider: bicep
  path: infra
  module: main
